{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd /home/claude/repo && git add -A && git diff --cached --quiet || (git commit -m 'Auto-push: update website' && git push)",
            "statusMessage": "Pushing changes to GitHub..."
          }
        ]
      }
    ]
  }
}
